name: CORE

on:
  push:

jobs:

  pass:
    strategy:
      matrix:
        os: [ubuntu-latest, macos-latest, windows-latest]

    runs-on: ${{ matrix.os }}

    steps:
      - name: Checkout [${{ github.repository }}]
        uses: actions/checkout@v4

      - name: Test a project
        uses: ./actions/uv/core
        with:
          python-version: 3.12
          working-directory: .github/hello-world
